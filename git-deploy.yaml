
# Example configuration
deploys:

  # Name of the job (for logging purposes)
  - name: website

    # List of branches this job runs on
    branches: [ master ]

    # Enviroment variables used in the building process
    env:
      MODE:           production
      HIDE_PRIDE:     true
      HIDE_SKILLS:    false
      HIDE_PLATFORMS: false

    # pre and post commands
    pre:
    post:

    # The deployer and its settings
    # use: script
    # with:
    #   script: ./build.sh
    #   shell: bash
   
    #use: ./build.sh
    
    use: npm
    # with:
    #   script: build
